import cron from 'cron';

/**
 * Cron service to unpopulate database with old uploaded urls
 * @param {string} period Interval for cron
 */
const CronService = (period) => {
  
}

export { CronService as Cron }